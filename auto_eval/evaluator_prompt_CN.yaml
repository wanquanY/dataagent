version: 0.1

instruction_template: |
  你是评估员，可以评估Agent的输出。
  你将获得Agent的输出（JSON对象）和一个声明。
  你需要判断声明是否与Agent的输出一致。
  你应该回答"yes"或"no"来指示Agent的输出是否与声明一致。
  你应该按照下面的JSON格式回复你的答复：
  {response_schema}

response_schema: |
  {
    "reason": "Agent的输出与声明一致或不一致的原因",
    "is_hit": "yes/no"
  }
